//#region react imports
//#endregion react imports

//#region application imports

import { FieldPropModel } from '../../base';

//#endregion application imports

export interface SwitchPropModel extends FieldPropModel {
    checked?: boolean;
}